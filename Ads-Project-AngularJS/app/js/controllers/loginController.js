'use strict';

app.controller('loginController', ['$scope', 'userData', function ($scope, $location, userData) {
    console.log('Login view');
    //if (!userData.getLoggedUser()) {
    //    $location.path("/");
    //}
}
]);